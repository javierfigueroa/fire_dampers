<div class="container">
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {:class => "form-horizontal"}) do |f| %>
  <fieldset>
  	<legend>Sign in</legend>
  <div class="control-group"><%= f.label :email , :class => 'control-label' %><div class="controls">
  <%= f.email_field :email %></div></div>

  <div class="control-group"><%= f.label :password , :class => 'control-label' %><div class="controls">
  <%= f.password_field :password %></div></div>
<!-- 
  <% if devise_mapping.rememberable? -%>
    <div class="control-group"> <%= f.label :remember_me , :class => 'control-label' %><div class="controls"><%= f.check_box :remember_me %></div></div>
  <% end -%> -->

  <div class="form-actions"><%= f.submit "Sign in", :class => 'btn btn-primary' %>
           <!-- <a href="/password/new">Forgot your password?</a> --></div>
  </fieldset>
<% end %></div>
<!-- 
<%= render "links" %> -->